<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Madkting user Group -->
    <record id="madkting_user_group" model="res.groups">
        <field name="name">Madkting User</field>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <!-- Madkting api user Group -->
    <record id="madkting_api_group" model="res.groups">
        <field name="name">Madkting API</field>
        <field name="implied_ids" eval="[(4, ref('madkting_user_group')),
                      (4, ref('sales_team.group_sale_manager')),
                      (4, ref('stock.group_stock_manager')),
                      (4, ref('account.group_account_manager')),
                      (4, ref('account.group_account_invoice')),
                      (4, ref('account.group_account_user'))]"/>
    </record>

</odoo>